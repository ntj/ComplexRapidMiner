<operator name="Root" class="Process">
  <description text="RemoveUselessAttributes is one of the very simple feature preprocessing operators. It simply removes such attributes only providing a very small deviation. The default settings only removes those features which have the same value for all examples."/>
  <parameter key="logverbosity"	value="warning"/>
  <operator name="ExampleSource" class="ExampleSource">
    <parameter key="attributes"	value="../data/sonar.aml"/>
  </operator>
  <operator name="RemoveUselessAttributes" class="RemoveUselessAttributes">
    <parameter key="numerical_min_deviation"	value="0.1"/>
  </operator>
</operator>

