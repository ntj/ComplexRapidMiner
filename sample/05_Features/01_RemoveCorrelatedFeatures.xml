<operator name="Root" class="Process">
  <description text="RemoveCorrelatedFeatures is one of the very simple feature preprocessing operators. It simply removes such attributes highly correlated to other features."/>
  <parameter key="logverbosity"	value="warning"/>
  <operator name="ExampleSource" class="ExampleSource">
    <parameter key="attributes"	value="../data/sonar.aml"/>
  </operator>
  <operator name="RemoveCorrelatedFeatures" class="RemoveCorrelatedFeatures">
    <parameter key="correlation"	value="0.5"/>
  </operator>
</operator>

